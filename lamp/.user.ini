; ============================================================
; .user.ini — Configuración PHP por directorio
; PHP lee este archivo automáticamente en cada request
; ============================================================

; Activa output buffering global — evita "headers already sent"
; aunque haya BOM u output antes del <?php
output_buffering = On

; Tamaño del buffer (4KB suficiente)
output_handler =
